'use strict';

var Class   = require('appolo-express').Class,
    _       = require('lodash'),
    Q       = require('q');

module.exports = Class.define({

    $config: {
        namespace: 'Stox.Enums',
        statics: {

        }
    }
});